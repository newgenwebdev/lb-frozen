# Prevent React types from being hoisted to root
# This allows server (React 18) and storepage/admin (React 19) to use their own versions
public-hoist-pattern[]=!@types/react
public-hoist-pattern[]=!@types/react-dom

# Prevent tailwindcss from being hoisted to root
# This allows server (v3) and storepage/admin (v4) to use their own versions
public-hoist-pattern[]=!tailwindcss
