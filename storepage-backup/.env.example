# ============================================================
# LB FROZEN FRONTEND ENVIRONMENT CONFIGURATION
# ============================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control!
# ============================================================

# === MEDUSA BACKEND ===
# URL of your Medusa backend API
# Development: http://localhost:9000
# Production: https://your-backend-domain.com
NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9000

# Medusa publishable API key
# Get from Medusa admin: Settings > API Keys > Publishable
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_API_KEY=pk_your_publishable_key_here

# === FILE STORAGE ===
# Hostname for product images (S3/MinIO bucket)
# Used in next.config.ts for image optimization
NEXT_PUBLIC_IMAGE_HOSTNAME=your-bucket-hostname.com

# === STRIPE ===
# Stripe PUBLISHABLE key (safe for client-side)
# Get from: https://dashboard.stripe.com/apikeys
# Use pk_test_ for development, pk_live_ for production
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# ============================================================
# SECURITY NOTES:
# ============================================================
# - All NEXT_PUBLIC_ variables are exposed to the browser
# - Only use PUBLISHABLE keys here, never SECRET keys
# - Medusa publishable key is safe to expose (limited permissions)
# - Stripe publishable key is safe to expose (can only create tokens)
# ============================================================
